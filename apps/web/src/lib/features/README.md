# Web feature libraries

Domain API facades and workflow helpers live in the matching feature folder. The base API client, query-key factories, shared display helpers, and cross-feature utilities remain directly under `lib`.
