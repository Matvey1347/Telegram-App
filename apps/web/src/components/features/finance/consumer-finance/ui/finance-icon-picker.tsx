"use client";

// Consumer Finance uses the application-wide picker so emoji selection has the
// same icon-only category navigation, localized search and populated emoji grid
// as the rest of the product.
export { IconPicker } from "@/components/icons/icon-picker";
